---
phase: 5
plan: 3
wave: 3
depends_on: ["5.2"]
files_modified: []
autonomous: false
user_setup:
  - service: "Local Dev Environment"
    why: "End-to-end testing requires running both backend and frontend environments concurrently"

must_haves:
  truths:
    - "System functions end-to-end in a manual walkthrough"
  artifacts: []
---

# Plan 5.3: End-to-End Integration Test

<objective>
Start both backend and frontend, and manually verify the full 10-step negotiation flow.

Purpose: To ensure the AI pipeline, WebSockets, AudioWorklets, and UI components all function together correctly.
Output: Manual verification complete.
</objective>

<context>
Load for context:
- .planning/TASKS.md (Task 5-03)
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>End-to-end integration test</name>
  <files></files>
  <action>
    Instruct the user to start the backend locally:
      `cd backend && .\venv\Scripts\activate && uvicorn app.main:app --reload --host 0.0.0.0 --port 8080`
    And start the frontend locally (in a new terminal):
      `cd frontend && npm run dev`

    The user must walk through the following 10 steps manually:
      1. Open http://localhost:3000/negotiate
      2. Accept privacy consent (choose "live" mode)
      3. Enable camera — verify live video feed appears
      4. Click Start Negotiation — use context: "buying a used laptop, seller asking $500"
      5. Speak: "I'm interested in this laptop, what's your best price?"
      6. Verify: transcript panel updates with user speech
      7. Verify: strategy panel updates with AI recommendation and target price
      8. Verify: audio response plays back through the speaker
      9. Click End Negotiation
      10. Verify: OUTCOME_SUMMARY modal appears with deal analysis
    
    Fix any integration issues that arise during this process before marking it complete.
  </action>
  <verify>Manual execution of the 10 steps above.</verify>
  <done>User confirms all 10 steps passed successfully without errors.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Manual walkthrough passed
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
