---
phase: 5
plan: 1
wave: 1
depends_on: []
files_modified:
  - backend/app/services/negotiation_engine.py
autonomous: true

must_haves:
  truths:
    - "Gemini text responses containing strategy JSON are parsed and sent as STRATEGY_UPDATE"
    - "Non-strategy text from Gemini is sent as AI_RESPONSE"
    - "A context summary can be built for session handoffs"
  artifacts:
    - "backend/app/services/negotiation_engine.py contains handle_gemini_text and _build_context_summary"
---

# Plan 5.1: Backend Intelligence Parsing

<objective>
Implement parsing of `<strategy>` JSON blocks from Gemini text streaming and build the context summary for session handoff.

Purpose: To extract structured strategy data from the LLM and pass it to the frontend, and to allow seamless 10-minute session renewals.
Output: Updated `negotiation_engine.py` with parsing logic and context summary building.
</objective>

<context>
Load for context:
- .planning/codebase/SYSTEM_PROMPT.md
- backend/app/services/negotiation_engine.py
</context>

<tasks>

<task type="auto">
  <name>Add strategy parser and context summary</name>
  <files>backend/app/services/negotiation_engine.py</files>
  <action>
    Add `handle_gemini_text(websocket, session_id, text)`:
      - The `google-genai` SDK handles re-assembling chunks for `part.text`. No manual text buffering is required.
      - Use `STRATEGY_PATTERN = re.compile(r'<strategy>(.*?)</strategy>', re.DOTALL)` on the complete text.
      - Extract all strategy blocks, parse as JSON, send `STRATEGY_UPDATE` WebSocket message with payload containing the strategy data.
      - Strip strategy blocks from text, send any remaining text as `AI_RESPONSE` (with response_type="coaching" and a timestamp).
      - Catch JSONDecodeError non-fatally (log warning/discard to reuse previous strategy), do not crash session.
    Add `_build_context_summary(session: NegotiationSession) -> str`:
      - Build continuation context including original context, last strategy, and last 10 transcript entries.
      - This function produces the injected context that enables `monitor_session_lifetime` to transparently close and re-open connections at the 9-minute mark.
    AVOID: Replacing or wiping out existing code in `negotiation_engine.py`. Append these new helper functions.
  </action>
  <verify>cd backend; python -c "from app.services.negotiation_engine import handle_gemini_text, _build_context_summary; print('OK')"</verify>
  <done>Functions exist and can be imported successfully.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] `handle_gemini_text` is defined and correctly extracts `<strategy>` JSON
- [ ] `_build_context_summary` is defined and uses the session attributes correctly
- [ ] `re`, `json`, and `time` are imported if used
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
