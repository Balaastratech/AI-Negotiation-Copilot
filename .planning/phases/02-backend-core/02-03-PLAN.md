---
phase: 02-backend-core
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - backend/app/services/negotiation_engine.py
  - backend/app/api/websocket.py
  - backend/app/main.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "WebSocket connections at /ws receive CONNECTION_ESTABLISHED with session_id"
    - "State machine rejects invalid messages with appropriate ERROR codes"
    - "PRIVACY_CONSENT_GRANTED transitions IDLE → CONSENTED"
    - "START_NEGOTIATION opens Gemini Live session and transitions to ACTIVE"
    - "AUDIO_CHUNK and VISION_FRAME messages are forwarded to Gemini in ACTIVE state"
    - "END_NEGOTIATION computes outcome and sends OUTCOME_SUMMARY"
  artifacts:
    - path: "backend/app/services/negotiation_engine.py"
      provides: "State machine enforcement and message handlers"
    - path: "backend/app/api/websocket.py"
      provides: "WebSocket endpoint at /ws"
    - path: "backend/app/main.py"
      provides: "FastAPI app with integrated WebSocket"
  key_links:
    - from: "WebSocket endpoint"
      to: "NegotiationEngine"
      via: "route_message() dispatcher"
    - from: "NegotiationEngine"
      to: "GeminiClient"
      via: "handle_start() calls open_live_session"
    - from: "NegotiationEngine"
      to: "ConnectionManager"
      via: "register/unregister for session tracking"
---

<objective>
Implement state machine handlers and WebSocket endpoint.

Purpose: Create the NegotiationEngine that enforces state transitions and handles all client messages, then wire it to the WebSocket endpoint.
Output: NegotiationEngine with all handlers, WebSocket endpoint at /ws
</objective>

<context>
@.planning/codebase/STATE_MACHINE.md
@.planning/codebase/API_SCHEMAS.md
@.planning/codebase/GEMINI_SESSION.md
@backend/app/models/negotiation.py
@backend/app/models/messages.py
@backend/app/services/connection_manager.py
@backend/app/services/gemini_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NegotiationEngine with state machine</name>
  <files>backend/app/services/negotiation_engine.py</files>
  <action>
    Create NegotiationEngine with:
    - VALID_MESSAGES dict mapping state to allowed message types
    - ERROR_CODES dict for each state
    - validate_message(websocket, session, message_type) - checks if message valid in current state, sends ERROR if not
    - transition_state(session, new_state, websocket) - logs and applies transition
    - State machine handlers:
      - handle_consent(session, payload) - IDLE → CONSENTED
      - handle_start(session, payload, websocket, api_key) - CONSENTED → ACTIVE, opens Gemini session
      - handle_vision_frame(session, payload) - sends to Gemini
      - handle_audio_chunk(session, raw_bytes) - sends PCM to Gemini
      - handle_end(session, payload, websocket) - ACTIVE → ENDING → IDLE, computes outcome
    - route_message(websocket, session, msg_type, payload) - dispatcher
  </action>
  <verify>
    <automated>python -c "from app.services.negotiation_engine import NegotiationEngine, VALID_MESSAGES; print('Engine OK')"</automated>
  </verify>
  <done>All state transitions enforced, all message handlers implemented</done>
</task>

<task type="auto">
  <name>Task 2: Create WebSocket endpoint</name>
  <files>backend/app/api/websocket.py</files>
  <action>
    Create `backend/app/api/__init__.py` first (empty).
    Then create `backend/app/api/websocket.py` with:
    - @app.websocket("/ws") endpoint
    - Generates UUID session_id, creates NegotiationSession
    - Distinguishes binary (audio) from text (JSON) frames
    - Calls validate_message before processing each message
    - Calls route_message for JSON messages, handle_audio_chunk for binary
    - Handles WebSocketDisconnect, sends ERROR on exceptions
    - Imports ConnectionManager singleton, GeminiClient, NegotiationEngine
  </action>
  <verify>
    <automated>python -c "from app.api.websocket import router; print('WebSocket OK')"</automated>
  </verify>
  <done>WebSocket endpoint handles all message types, proper error handling</done>
</task>

<task type="auto">
  <name>Task 3: Wire WebSocket to main app</name>
  <files>backend/app/main.py</files>
  <action>
    Update main.py to:
    - Import the websocket router from app.api.websocket
    - Include the router in the FastAPI app (or add the websocket endpoint directly)
    - Ensure connection_manager is initialized as a singleton
  </action>
  <verify>
    <automated>python -c "from app.main import app; print('App loads WebSocket OK')"</automated>
  </verify>
  <done>FastAPI app has /ws endpoint registered, starts without errors</done>
</task>

</tasks>

<verification>
- All components importable and integrated
- uvicorn can start the app
- State machine enforces valid transitions per STATE_MACHINE.md
</verification>

<success_criteria>
Complete working WebSocket server that:
- Accepts WebSocket connections at /ws
- Enforces state machine (IDLE → CONSENTED → ACTIVE → ENDING → IDLE)
- Opens Gemini Live sessions on START_NEGOTIATION
- Sends/receives audio and vision
- Computes OUTCOME_SUMMARY on END_NEGOTIATION
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-core/02-03-SUMMARY.md`
</output>
