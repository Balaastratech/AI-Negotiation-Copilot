---
phase: 02-backend-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/negotiation.py
  - backend/app/models/messages.py
  - backend/app/services/connection_manager.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Session state can be tracked through IDLE → CONSENTED → ACTIVE → ENDING → IDLE lifecycle"
    - "WebSocket connections can be registered and tracked by session_id"
    - "Client and server message payloads are validated against schemas"
  artifacts:
    - path: "backend/app/models/negotiation.py"
      provides: "NegotiationSession model, NegotiationState enum"
    - path: "backend/app/models/messages.py"
      provides: "All request/response message schemas"
    - path: "backend/app/services/connection_manager.py"
      provides: "ConnectionManager for tracking active sessions"
  key_links:
    - from: "ConnectionManager"
      to: "NegotiationSession"
      via: "stores session objects in active_connections dict"
---

<objective>
Create foundational data models and connection management for WebSocket sessions.

Purpose: Establish the core Pydantic models for session state and message schemas, plus the ConnectionManager that tracks active WebSocket connections.
Output: NegotiationSession model, message schemas, ConnectionManager service
</objective>

<context>
@.planning/codebase/STATE_MACHINE.md
@.planning/codebase/API_SCHEMAS.md
@backend/app/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NegotiationSession model</name>
  <files>backend/app/models/negotiation.py</files>
  <action>
    Create `backend/app/models/__init__.py` first (empty with model exports).
    Then create `backend/app/models/negotiation.py` with:
    - NegotiationState enum: IDLE, CONSENTED, ACTIVE, ENDING
    - NegotiationSession pydantic model with: session_id, state, consent_version, consent_mode, started_at, context, live_session (Optional), initial_price, final_price, transcript, strategy_history
    - Use `arbitrary_types_allowed = True` for live_session handle
  </action>
  <verify>
    <automated>python -c "from app.models.negotiation import NegotiationSession, NegotiationState; print('Models OK')"</automated>
  </verify>
  <done>NegotiationSession model importable, NegotiationState enum has all 4 states</done>
</task>

<task type="auto">
  <name>Task 2: Create message schemas</name>
  <files>backend/app/models/messages.py</files>
  <action>
    Create message schemas matching API_SCHEMAS.md exactly:
    - ConsentPayload: version, mode
    - StartNegotiationPayload: context
    - VisionFramePayload: image (base64), timestamp
    - EndNegotiationPayload: final_price, initial_price
    - TranscriptEntry: id, speaker, text, timestamp
    - StrategyUpdate: target_price, current_offer, recommended_response, key_points, approach_type, confidence, walkaway_threshold, web_search_used, search_sources
    - OutcomeSummary: deal_reached, initial_price, final_price, savings, savings_percentage, market_value, vs_market, negotiation_duration_seconds, key_moves, effectiveness_score, transcript_summary
    - Server messages: ConnectionEstablished, ConsentAcknowledged, SessionStarted, TranscriptUpdate, StrategyUpdate, AIResponse, OutcomeSummary, AudioInterrupted, SessionReconnecting, AIDegraded, ErrorPayload
  </action>
  <verify>
    <automated>python -c "from app.models.messages import ConsentPayload, StartNegotiationPayload, SessionStarted; print('Schemas OK')"</automated>
  </verify>
  <done>All message schemas from API_SCHEMAS.md implemented</done>
</task>

<task type="auto">
  <name>Task 3: Create ConnectionManager service</name>
  <files>backend/app/services/connection_manager.py</files>
  <action>
    Create `backend/app/services/__init__.py` first (empty).
    Then create `backend/app/services/connection_manager.py` with:
    - ConnectionManager class with active_connections: dict[str, dict]
    - register(websocket, session_id, session) method
    - unregister(session_id) method - handles closing Gemini session if open
    - get_session(session_id) -> NegotiationSession | None
    - get_websocket(session_id) -> WebSocket | None
  </action>
  <verify>
    <automated>python -c "from app.services.connection_manager import ConnectionManager; cm = ConnectionManager(); print('ConnectionManager OK')"</automated>
  </verify>
  <done>ConnectionManager tracks sessions, unregister properly closes Gemini sessions</done>
</task>

</tasks>

<verification>
- All models importable without errors
- ConnectionManager instantiated and methods callable
</verification>

<success_criteria>
Phase ready for Plan 02: GeminiClient integration
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-core/02-01-SUMMARY.md`
</output>
