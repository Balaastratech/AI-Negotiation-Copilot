---
phase: 02-backend-core
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - backend/app/services/gemini_client.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Gemini Live sessions can be opened with AUDIO and TEXT modalities"
    - "Audio chunks from frontend can be sent to Gemini in real-time"
    - "Vision frames from frontend can be sent to Gemini in real-time"
    - "Gemini responses (audio + text) are received and forwarded to frontend"
    - "Session handoff occurs at 9 minutes to prevent 10-minute limit cutoff"
  artifacts:
    - path: "backend/app/services/gemini_client.py"
      provides: "GeminiClient service with open_live_session, send_audio_chunk, send_vision_frame, receive_responses"
  key_links:
    - from: "GeminiClient"
      to: "google.genai.Client"
      via: "client.aio.live.connect() for Live API"
    - from: "GeminiClient"
      to: "WebSocket"
      via: "receive_responses sends audio/text to frontend via websocket.send_bytes/send_json"
---

<objective>
Implement Gemini Live API client with session management.

Purpose: Create the GeminiClient service that handles opening Live sessions, sending audio/video, receiving responses, and session handoff for the 9-minute limit.
Output: GeminiClient service with open_live_session, send_audio_chunk, send_vision_frame, receive_responses, monitor_session_lifetime
</objective>

<context>
@.planning/codebase/GEMINI_SESSION.md
@.planning/codebase/STATE_MACHINE.md
@.planning/codebase/API_SCHEMAS.md
@backend/app/models/negotiation.py
@backend/app/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GeminiClient with open_live_session</name>
  <files>backend/app/services/gemini_client.py</files>
  <action>
    Create GeminiClient service following GEMINI_SESSION.md exactly:
    - Use google.genai (NOT google.generativeai)
    - GEMINI_MODEL_PRIMARY = "gemini-2.5-flash-native-audio-preview-12-2025"
    - GEMINI_MODEL_FALLBACK = "gemini-2.0-flash-live-preview-04-09"
    - open_live_session() async context manager with model fallback chain
    - LiveConnectConfig with response_modalities=["AUDIO", "TEXT"], Google Search tool, input/output transcription
    - GeminiUnavailableError exception class
  </action>
  <verify>
    <automated>python -c "from app.services.gemini_client import GeminiClient, GeminiUnavailableError; print('GeminiClient OK')"</automated>
  </verify>
  <done>open_live_session works as async context manager, fallback chain implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement send_audio_chunk and send_vision_frame</name>
  <files>backend/app/services/gemini_client.py</files>
  <action>
    Add to GeminiClient:
    - send_audio_chunk(live_session, raw_pcm_bytes, session_id) - sends PCM 16kHz to Gemini
    - send_vision_frame(live_session, jpeg_base64, session_id) - decodes base64, sends as video blob
    - Both methods handle exceptions gracefully (log warning, don't raise)
  </action>
  <verify>
    <automated>python -c "from app.services.gemini_client import GeminiClient; print('Send methods OK')"</automated>
  </verify>
  <done>Audio and vision sending methods implemented with error handling</done>
</task>

<task type="auto">
  <name>Task 3: Implement receive_responses and session lifetime monitor</name>
  <files>backend/app/services/gemini_client.py</files>
  <action>
    Add to GeminiClient:
    - receive_responses(live_session, websocket, session_id) - async loop receiving Gemini responses, handling: server_content.model_turn (audio + text), input_transcription, output_audio_transcription, interrupted signal
    - SESSION_HARD_LIMIT_SECONDS = 600, SESSION_HANDOFF_TRIGGER = 540
    - monitor_session_lifetime(session, websocket, api_key) - background task that at 9min triggers handoff
  </action>
  <verify>
    <automated>python -c "from app.services.gemini_client import SESSION_HANDOFF_TRIGGER; assert SESSION_HANDOFF_TRIGGER == 540; print('Monitor OK')"</automated>
  </verify>
  <done>Receive loop handles all response types, session handoff at 9 minutes</done>
</task>

</tasks>

<verification>
- GeminiClient instantiates without errors
- All session methods implemented per GEMINI_SESSION.md specs
</verification>

<success_criteria>
Phase ready for Plan 03: NegotiationEngine and WebSocket endpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-core/02-02-SUMMARY.md`
</output>
