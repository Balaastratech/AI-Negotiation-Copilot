---
phase: 01-project-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - Scaffolding
user_setup: []

must_haves:
  truths:
    - "Project directory structure matches STRUCTURE.md"
    - "Frontend package.json with all required dependencies"
    - "Backend requirements.txt with correct google-genai SDK"
    - "Backend Dockerfile with proper configuration"
    - "Backend app config.py with pydantic-settings"
    - "Backend main.py FastAPI app running"
  artifacts:
    - path: "frontend/app/"
      provides: "Next.js app router directory"
    - path: "frontend/components/negotiation/"
      provides: "Negotiation components directory"
    - path: "frontend/components/ui/"
      provides: "UI primitives directory"
    - path: "frontend/components/providers/"
      provides: "React context providers directory"
    - path: "frontend/lib/"
      provides: "Core utilities directory"
    - path: "frontend/hooks/"
      provides: "Custom hooks directory"
    - path: "frontend/public/worklets/"
      provides: "AudioWorklet files location (CRITICAL)"
    - path: "backend/app/api/"
      provides: "FastAPI routes directory"
    - path: "backend/app/services/"
      provides: "Business logic services directory"
    - path: "backend/app/models/"
      provides: "Pydantic models directory"
    - path: "backend/app/utils/"
      provides: "Helper utilities directory"
    - path: "backend/app/middleware/"
      provides: "Middleware directory"
    - path: "backend/tests/"
      provides: "Backend tests directory"
    - path: "infrastructure/"
      provides: "Deployment configs directory"
    - path: "docs/"
      provides: "Documentation directory"
    - path: "scripts/"
      provides: "Utility scripts directory"
  key_links: []
---

<objective>
Create working project structure with all configs, dependencies, and empty entry points for Phase 1 (Project Scaffolding).

Purpose: Establish the foundation for all subsequent phases. Wrong SDK in requirements.txt is the TOP concern - must use google-genai NOT google-generativeai.

Output: Complete directory structure, frontend configs, backend requirements.txt, Dockerfile, config.py, and main.py
</objective>

<execution_context>
@./.planning/codebase/TASKS.md
@./.planning/codebase/STRUCTURE.md
@./.planning/codebase/STACK.md
@./.planning/codebase/CONCERNS.md
</execution_context>

<context>
## Phase 1 Tasks from TASKS.md

### Task 1-01: Create complete directory structure
- Create all directories exactly as specified in .planning/codebase/STRUCTURE.md
- Key directories include: frontend/app/, frontend/components/negotiation/, frontend/components/ui/, frontend/components/providers/, frontend/lib/, frontend/hooks/, frontend/public/worklets/, backend/app/api/, backend/app/services/, backend/app/models/, backend/app/utils/, backend/app/middleware/, backend/tests/, infrastructure/, docs/, scripts/

### Task 1-02: Create frontend package.json and config files
- Create frontend/package.json with: Next.js 14, React 18, TypeScript, TailwindCSS, lucide-react
- Exact versions from STACK.md
- Create frontend/tsconfig.json: strict mode enabled, path alias @/* -> ./*
- Create frontend/next.config.js
- Create frontend/tailwind.config.js

### Task 1-03: Create backend requirements.txt and Dockerfile
- Create backend/requirements.txt with:
  - fastapi>=0.109.0
  - uvicorn[standard]>=0.27.0
  - websockets>=12.0
  - google-genai>=1.0.0 (CORRECT SDK - NOT google-generativeai)
  - pydantic>=2.0.0
  - pydantic-settings>=2.0.0
  - python-dotenv>=1.0.0
  - pillow>=10.0.0
- Create backend/Dockerfile with python:3.11-slim, healthcheck, proper user setup
- Create backend/.env.example with all required env vars

### Task 1-04: Create backend entry point and config
- Create backend/app/__init__.py (empty)
- Create backend/app/config.py using pydantic-settings BaseSettings
- Create backend/app/main.py with FastAPI app, CORS, health router
</context>

<tasks>

<task type="auto">
  <name>Task 1-01: Create complete directory structure</name>
  <files>frontend/app/, frontend/components/negotiation/, frontend/components/ui/, frontend/components/providers/, frontend/lib/, frontend/hooks/, frontend/public/worklets/, backend/app/api/, backend/app/services/, backend/app/models/, backend/app/utils/, backend/app/middleware/, backend/tests/, infrastructure/, docs/, scripts/</files>
  <action>
Create all directories exactly as specified in .planning/codebase/STRUCTURE.md:
- frontend/app/ - Next.js App Router pages
- frontend/components/negotiation/ - Domain-specific React components
- frontend/components/ui/ - Reusable UI primitives
- frontend/components/providers/ - React Context providers
- frontend/lib/ - Core utilities and clients
- frontend/hooks/ - Custom React hooks
- frontend/public/worklets/ - CRITICAL: AudioWorklet .js files MUST live here, not src/ or lib/
- backend/app/api/ - API route handlers
- backend/app/services/ - Business logic services
- backend/app/models/ - Pydantic data models
- backend/app/utils/ - Helper utilities
- backend/app/middleware/ - Middleware components
- backend/tests/ - Backend tests
- infrastructure/ - Deployment configs
- docs/ - Documentation
- scripts/ - Utility scripts
  </action>
  <verify>
    <automated>powershell: Test-Path frontend/public/worklets; Test-Path backend/app/services; Test-Path frontend/app; Test-Path backend/tests</automated>
  </verify>
  <done>All required directories created as per STRUCTURE.md</done>
</task>

<task type="auto">
  <name>Task 1-02: Create frontend package.json and config files</name>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/next.config.js, frontend/tailwind.config.js</files>
  <action>
Create frontend/package.json with:
- next: ^14.0.0
- react: ^18.2.0
- react-dom: ^18.2.0
- typescript: ^5.0.0
- tailwindcss: ^3.4.0
- lucide-react: ^0.300.0
- @types/node: ^20.0.0
- @types/react: ^18.2.0
- @types/react-dom: ^18.2.0

Create frontend/tsconfig.json with:
- strict mode enabled
- path alias @/* -> ./*
- Next.js compiler options

Create frontend/next.config.js with standard Next.js config.

Create frontend/tailwind.config.js with:
- content paths for app, components, lib, hooks
- TailwindCSS theme defaults
  </action>
  <verify>
    <automated>powershell: Test-Path frontend/package.json; Test-Path frontend/tsconfig.json</automated>
  </verify>
  <done>Frontend package.json with correct dependencies, tsconfig with strict mode, next.config.js, tailwind.config.js all created</done>
</task>

<task type="auto">
  <name>Task 1-03: Create backend requirements.txt and Dockerfile</name>
  <files>backend/requirements.txt, backend/Dockerfile, backend/.env.example</files>
  <action>
Create backend/requirements.txt with EXACT versions:
- fastapi>=0.109.0
- uvicorn[standard]>=0.27.0
- websockets>=12.0
- google-genai>=1.0.0  (CRITICAL: Use google-genai NOT google-generativeai)
- pydantic>=2.0.0
- pydantic-settings>=2.0.0
- python-dotenv>=1.0.0
- pillow>=10.0.0

Create backend/Dockerfile:
- FROM python:3.11-slim
- WORKDIR /app
- COPY requirements.txt .
- RUN pip install --no-cache-dir -r requirements.txt
- COPY . .
- RUN useradd -m -u 1000 appuser && chown -R appuser /app
- USER appuser
- HEALTHCHECK --interval=30s --timeout=5s CMD curl -f http://localhost:8080/health || exit 1
- EXPOSE 8080
- CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8080"]

Create backend/.env.example with:
- GEMINI_API_KEY=your_key_here
- GEMINI_MODEL=gemini-2.5-flash-native-audio-preview-12-2025
- GEMINI_MODEL_FALLBACK=gemini-2.0-flash-live-preview-04-09
- CORS_ORIGINS=http://localhost:3000
- LOG_LEVEL=INFO
- SESSION_TTL_SECONDS=3600
  </action>
  <verify>
    <automated>powershell: Select-String "google-genai" backend/requirements.txt</automated>
  </verify>
  <done>requirements.txt with correct google-genai SDK, Dockerfile with healthcheck, .env.example all created</done>
</task>

<task type="auto">
  <name>Task 1-04: Create backend entry point and config</name>
  <files>backend/app/__init__.py, backend/app/config.py, backend/app/main.py</files>
  <action>
Create backend/app/__init__.py (empty file)

Create backend/app/config.py using pydantic-settings:
- GEMINI_API_KEY: str (from env)
- GEMINI_MODEL: str = "gemini-2.5-flash-native-audio-preview-12-2025"
- GEMINI_MODEL_FALLBACK: str = "gemini-2.0-flash-live-preview-04-09"
- CORS_ORIGINS: list[str] = ["http://localhost:3000"]
- LOG_LEVEL: str = "INFO"
- SESSION_TTL_SECONDS: int = 3600
- Config class extends BaseSettings

Create backend/app/main.py:
- FastAPI app instance with title="AI Negotiation Copilot", version="1.0.0"
- CORSMiddleware (allow all origins in dev)
- Health router at /api/health
- WebSocket router at /ws (placeholder for now)
- Startup log showing active model name
- Include a basic /health endpoint that returns {"status": "healthy"}
  </action>
  <verify>
    <automated>powershell: cd backend; python -c "from app.main import app; print('OK')"</automated>
  </verify>
  <done>Backend entry point and config created, FastAPI app can be imported without errors</done>
</task>

</tasks>

<verification>
- All directories created matching STRUCTURE.md
- frontend/package.json exists with required dependencies
- backend/requirements.txt contains google-genai (NOT google-generativeai)
- backend/Dockerfile created with healthcheck
- backend/app/config.py uses pydantic-settings
- backend/app/main.py imports successfully (python -c "from app.main import app")
</verification>

<success_criteria>
- Phase 1 complete when all 4 tasks are done
- Directory structure matches STRUCTURE.md exactly
- Frontend configs are valid (Next.js 14, React 18, TypeScript, TailwindCSS)
- Backend requirements.txt has correct google-genai SDK
- Backend Dockerfile has healthcheck configured
- Backend FastAPI app starts without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffolding/01-01-SUMMARY.md`
</output>
