---
phase: 03-audio-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [frontend/lib/audio-worklet-manager.ts]
autonomous: true

must_haves:
  truths:
    - AudioWorkletManager can request microphone access and start capturing at 16kHz
    - AudioWorkletManager can instantiate the playback context at 24kHz
    - AudioWorkletManager can correctly route 24kHz PCM chunks to the playback worklet
    - AudioWorkletManager exposes a clean cleanup method that releases all hardware locks
  artifacts:
    - frontend/lib/audio-worklet-manager.ts
  key_links:
    - The manager successfully registers the worklet modules by URL (`/worklets/pcm-processor.js` and `/worklets/pcm-playback-processor.js`) using `addModule()`
---

<objective>
Implement the `AudioWorkletManager` TypeScript class that bridges the browser's `AudioContext` with the custom PCM worklets created in Plan 01.

Purpose: The frontend UI components need a clean, type-safe API to start microphone capture, play back received audio, and handle component unmounting without leaking memory or microphone permissions.
Output: A new library module `frontend/lib/audio-worklet-manager.ts`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/AUDIO_PIPELINE.md
@.planning/phases/03-audio-pipeline/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AudioWorkletManager Capture</name>
  <files>frontend/lib/audio-worklet-manager.ts</files>
  <action>Create the `AudioWorkletManager` class. Implement `startCapture(onChunk)`. It must initialize an AudioContext at 16000Hz, load `/worklets/pcm-processor.js` via `addModule()`, request `getUserMedia({audio: true})`, and connect a `MediaStreamAudioSourceNode` to an `AudioWorkletNode` named 'pcm-capture-processor'.</action>
  <verify>Ensure `frontend/lib/audio-worklet-manager.ts` exports `AudioWorkletManager` and correctly forces `sampleRate: 16000`.</verify>
  <done>Manager can initialize the 16kHz capture pipeline successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AudioWorkletManager Playback</name>
  <files>frontend/lib/audio-worklet-manager.ts</files>
  <action>Add `initPlayback()` to the manager. It must initialize an AudioContext at 24000Hz, load `/worklets/pcm-playback-processor.js` via `addModule()`, and create an `AudioWorkletNode` named 'pcm-playback-processor'. Add `playChunk(chunk: ArrayBuffer)` to forward PCM chunks via `port.postMessage`.</action>
  <verify>Ensure `frontend/lib/audio-worklet-manager.ts` handles the 24kHz playback pipeline integration.</verify>
  <done>Manager can initialize and feed the 24kHz playback pipeline successfully.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Cleanup Methods</name>
  <files>frontend/lib/audio-worklet-manager.ts</files>
  <action>Add `stopCapture()` that stops all tracks on the MediaStream and disconnects capture nodes. Add `cleanup()` that fully closes both AudioContexts and drops strong references. Add a getter `isCapturing`.</action>
  <verify>Check that `cleanup()` thoroughly releases hardware and contexts to prevent browser memory leaks.</verify>
  <done>Manager safely releases microphone locks and garbage collects internal elements.</done>
</task>

</tasks>

<verification>
Static verification: The manager must import no React concepts (it is a pure standard lib class). The `addModule` paths must point to the literal string paths established in Plan 01.
</verification>

<success_criteria>
`frontend/lib/audio-worklet-manager.ts` is created and completely implements the interface specified in AUDIO_PIPELINE.md.
</success_criteria>

<output>
After completion, create `.planning/phases/03-audio-pipeline/03-02-SUMMARY.md`
</output>
