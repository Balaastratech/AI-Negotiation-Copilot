---
phase: 03-audio-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified: [frontend/lib/websocket.ts]
autonomous: true

must_haves:
  truths:
    - The WebSocket client connects to the backend API
    - The client correctly separates incoming binary frames (audio) and text frames (JSON state)
    - The client uses `audio-worklet-manager` to play binary audio frames
    - The client exposes a method to send Int16Array buffers directly as binary frames
  artifacts:
    - frontend/lib/websocket.ts
  key_links:
    - The `onmessage` handler explicitly checks for `event.data instanceof ArrayBuffer` (or `Blob`) before parsing JSON
---

<objective>
Implement the WebSocket client wrapper that bridges the `AudioWorkletManager` streams with the FastAPI backend, utilizing correct binary frame segregation for the raw PCM audio.

Purpose: Standard WebSockets easily mix text and binary data. We need a unified class that handles the connection lifecycle, routes incoming 24kHz PCM directly to the playback worklet queue, routes text events to UI callbacks, and allows streaming 16kHz PCM chunks directly to the backend without JSON encoding overhead.
Output: A new library module `frontend/lib/websocket.ts`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/AUDIO_PIPELINE.md
@.planning/phases/03-audio-pipeline/03-01-PLAN.md
@.planning/phases/03-audio-pipeline/03-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement WebSocket Connection & Core Methods</name>
  <files>frontend/lib/websocket.ts</files>
  <action>Create the `NegotiationWebSocket` class. Constructor takes a WebSocket URL and an existing `AudioWorkletManager` instance. Implement `connect()`, `disconnect()`, `sendAudioChunk(buffer)`, and `sendControl(type, payload)` methods.</action>
  <verify>Ensure `frontend/lib/websocket.ts` exports `NegotiationWebSocket` and correctly issues `ws.send()` calls using binary arrays vs JSON strings based on method.</verify>
  <done>Client establishes WebSocket connection and differentiates send methods.</done>
</task>

<task type="auto">
  <name>Task 2: Frame Demultiplexing</name>
  <files>frontend/lib/websocket.ts</files>
  <action>Implement the `ws.onmessage` handler. It MUST check if `event.data` is an ArrayBuffer or Blob. If binary, immediately forward it to `this.audioManager.playChunk()`. If string/JSON, parse it and emit it to registered control message listeners.</action>
  <verify>Check that `frontend/lib/websocket.ts` cleanly intercepts binary frames and does not crash attempting to `JSON.parse` raw audio data.</verify>
  <done>Incoming messages are safely routed to audio playback or UI state callbacks based on frame type.</done>
</task>

</tasks>

<verification>
Static verification: The class must rely on `AudioWorkletManager` for audio processing and explicitly handle `ArrayBuffer`/`Blob` resolution in the `ws.onmessage` handler as described in AUDIO_PIPELINE.md.
</verification>

<success_criteria>
`frontend/lib/websocket.ts` fully supports the binary audio + JSON control message architecture required by Gemini Live communication.
</success_criteria>

<output>
After completion, create `.planning/phases/03-audio-pipeline/03-03-SUMMARY.md`
</output>
