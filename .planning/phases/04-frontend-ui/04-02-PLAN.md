---
phase: 4
plan: 2
wave: 2
depends_on: ["04-01-PLAN.md"]
files_modified: ["frontend/components/negotiation/StrategyPanel.tsx", "frontend/components/negotiation/TranscriptPanel.tsx", "frontend/components/negotiation/ControlBar.tsx"]
autonomous: true

must_haves:
  truths:
    - "StrategyPanel accurately displays given strategy mock data"
    - "TranscriptPanel renders a list of chat messages correctly sorted"
    - "ControlBar has buttons to toggle mic/video and end negotiation"
  artifacts:
    - "frontend/components/negotiation/StrategyPanel.tsx"
    - "frontend/components/negotiation/TranscriptPanel.tsx"
    - "frontend/components/negotiation/ControlBar.tsx"
---

# Plan 4.2: Core Panels

<objective>
Build the core UI panels of the dashboard that will visualize the negotiation state using the types and mock data from Wave 1.
Purpose: To create the visual components needed by the main dashboard.
Output: StrategyPanel, TranscriptPanel, ControlBar.
</objective>

<context>
Load for context:
- .planning/codebase/API_SCHEMAS.md
- frontend/lib/types.ts
- frontend/lib/mock-data.ts
</context>

<tasks>

<task type="auto">
  <name>Build StrategyPanel component</name>
  <files>frontend/components/negotiation/StrategyPanel.tsx</files>
  <action>
    Create a component that accepts a `Strategy | null` prop (from `frontend/lib/types.ts`).
    If null, show a waiting/empty state.
    If populated, display the `recommended_response` prominently, list `key_points`, show `target_price` vs `current_offer`.
    Use Tailwind CSS for a clean, modern card look. Use lucide-react icons.
    AVOID: Modifying the data. The component should be strictly presentational.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>StrategyPanel.tsx is created and typechecks.</done>
</task>

<task type="auto">
  <name>Build TranscriptPanel component</name>
  <files>frontend/components/negotiation/TranscriptPanel.tsx</files>
  <action>
    Create a component that accepts `TranscriptEntry[]` as a prop.
    Render a chat-like interface displaying messages. Different colors/alignments based on `speaker` ('user', 'counterparty', 'ai').
    Ensure it scrolls to the bottom on new messages (use a basic ref approach).
    AVOID: Any markdown parsing unless simple. Just display text.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>TranscriptPanel.tsx is created and typechecks.</done>
</task>

<task type="auto">
  <name>Build ControlBar component</name>
  <files>frontend/components/negotiation/ControlBar.tsx</files>
  <action>
    Create a component for user controls.
    Needs props: `isAudioActive`, `isVisionActive`, callbacks `onToggleAudio`, `onToggleVision`, `onEndNegotiation`.
    Render professional control buttons (mic, camera, end call) using lucide-react.
    AVOID: Handling actual WebSockets or MediaStreams here.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>ControlBar.tsx is created and typechecks.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] StrategyPanel handles null state and populated state
- [ ] TranscriptPanel handles empty arrays and differentiates speakers
- [ ] ControlBar exposes standard callback props
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
