---
phase: 4
plan: 3
wave: 3
depends_on: ["04-01-PLAN.md", "04-02-PLAN.md"]
files_modified: ["frontend/components/negotiation/NegotiationDashboard.tsx", "frontend/app/negotiate/page.tsx"]
autonomous: true

must_haves:
  truths:
    - "NegotiationDashboard integrates all panels over a responsive layout"
    - "The /negotiate page renders the dashboard with mock data injected"
  artifacts:
    - "frontend/components/negotiation/NegotiationDashboard.tsx"
    - "frontend/app/negotiate/page.tsx"
---

# Plan 4.3: Dashboard Integration

<objective>
Assemble all the UI components built in Waves 1 & 2 into a cohesive dashboard and wire them up with mock data on the Next.js page.
Purpose: Complete the visual shell of the application so it is ready for Phase 5 (intelligence integration).
Output: NegotiationDashboard component and the main app/negotiate/page.tsx page.
</objective>

<context>
Load for context:
- .planning/codebase/STRUCTURE.md
- frontend/lib/types.ts
- frontend/lib/mock-data.ts
- frontend/components/negotiation/*
</context>

<tasks>

<task type="auto">
  <name>Build NegotiationDashboard component</name>
  <files>frontend/components/negotiation/NegotiationDashboard.tsx</files>
  <action>
    Create the master layout component that accepts the full `NegotiationState`.
    Include all sub-components (`PrivacyConsent`, `VideoCapture`, `StrategyPanel`, `TranscriptPanel`, `ControlBar`).
    If `state.consentGiven` is false, show `PrivacyConsent` covering everything.
    Otherwise, render a grid layout: Video/Transcript on the left, Strategy on the right, Controls at the bottom.
    AVOID: Including data-fetching logic. It should accept state and emit events (onToggleAudio, etc) up.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>NegotiationDashboard integrates all sub-components and typechecks.</done>
</task>

<task type="auto">
  <name>Wire up mock data in negotiate page</name>
  <files>frontend/app/negotiate/page.tsx</files>
  <action>
    Create a Next.js client component (`"use client"`) that holds the root `NegotiationState` using `useState`.
    Initialize it with `INITIAL_NEGOTIATION_STATE`. 
    Implement a temporary "Load Mock Data" button that populates the state using `MOCK_STRATEGY` and `MOCK_TRANSCRIPT` to verify the UI.
    Render the `NegotiationDashboard` and pass the state and handler callbacks.
    AVOID: Any real WebSocket or API calls just yet.
  </action>
  <verify>npm run build in `frontend/` (or equivalent Next.js typecheck check like `npx tsc --noEmit` on the frontend folder)</verify>
  <done>The negotiate app page renders successfully with mock state.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] The dashboard layout is responsive and logical
- [ ] When consent is given, the main interface appears
- [ ] Mock data toggling works to test the populated states
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
