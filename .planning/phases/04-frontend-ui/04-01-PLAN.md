---
phase: 4
plan: 1
wave: 1
depends_on: []
files_modified: ["frontend/lib/types.ts", "frontend/lib/mock-data.ts", "frontend/components/negotiation/PrivacyConsent.tsx", "frontend/components/negotiation/VideoCapture.tsx"]
autonomous: true

must_haves:
  truths:
    - "Application types exactly match API_SCHEMAS.md"
    - "Mock data arrays and objects match the defined types"
    - "Privacy consent UI matches the requirements"
  artifacts:
    - "frontend/lib/types.ts"
    - "frontend/lib/mock-data.ts"
    - "frontend/components/negotiation/PrivacyConsent.tsx"
    - "frontend/components/negotiation/VideoCapture.tsx"
---

# Plan 4.1: Types, Mock Data & Base Components

<objective>
Establish the core TypeScript definitions from the API Schema and build base UI components that don't depend on complex state.
Purpose: To ensure all downstream components share correct TypeScript definitions and have mock data to render.
Output: Core `types.ts`, `mock-data.ts`, and base stateless/low-state components (`PrivacyConsent`, `VideoCapture`).
</objective>

<context>
Load for context:
- .planning/codebase/API_SCHEMAS.md
- .planning/codebase/STRUCTURE.md
- .planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Create TypeScript types and mock data</name>
  <files>
    frontend/lib/types.ts
    frontend/lib/mock-data.ts
  </files>
  <action>
    Extract all TypeScript types provided in `.planning/codebase/API_SCHEMAS.md` and export them from `frontend/lib/types.ts`. 
    Create `frontend/lib/mock-data.ts` using the exact mock data provided in `API_SCHEMAS.md`.
    AVOID: modifying the API schemas or inventing new fields. The types must match the backend EXACTLY.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>types.ts and mock-data.ts exist and compile without errors.</done>
</task>

<task type="auto">
  <name>Build PrivacyConsent component</name>
  <files>frontend/components/negotiation/PrivacyConsent.tsx</files>
  <action>
    Create a React component using Tailwind CSS that displays a privacy consent notice.
    It should take an `onAccept` callback as a prop.
    It should explain that audio/video will be sent to AI.
    AVOID: Complex state management inside this component. Just present UI and trigger the callback.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>PrivacyConsent.tsx is created and typechecks.</done>
</task>

<task type="auto">
  <name>Build VideoCapture component interface</name>
  <files>frontend/components/negotiation/VideoCapture.tsx</files>
  <action>
    Create a React component using Tailwind CSS that displays a webcam preview box.
    For this phase, it can just be a placeholder UI (a black box with a camera icon from lucide-react) if the actual `navigator.mediaDevices.getUserMedia` logic is kept separate or simulated for now.
    Accept `isActive: boolean` and `onToggle: () => void`.
    AVOID: Connecting actual WebRTC state handling here if it makes testing hard. This is just the UI.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>VideoCapture.tsx is created and typechecks.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] `frontend/lib/types.ts` contains `NegotiationState`, `TranscriptEntry`, `Strategy`, etc.
- [ ] `frontend/lib/mock-data.ts` exports `MOCK_STRATEGY` and `MOCK_TRANSCRIPT`
- [ ] Both components export properly and have Tailwind classes applied
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
